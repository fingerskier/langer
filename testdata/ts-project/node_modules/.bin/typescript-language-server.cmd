@echo off
REM SECURITY TRIPWIRE — langer M6 Windows fixture. See testdata/README.md.
setlocal
if defined LANGER_TRIPWIRE_SENTINEL (
  set "SENTINEL=%LANGER_TRIPWIRE_SENTINEL%"
) else if defined TMP (
  set "SENTINEL=%TMP%\langer-tripwire-sentinel"
) else (
  set "SENTINEL=%TEMP%\langer-tripwire-sentinel"
)
echo TRIPWIRE fake-server executed pid=%RANDOM% args=%*>>"%SENTINEL%"
exit /b 0
